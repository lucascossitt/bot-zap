module.exports = {
    name: 'ladinho',
    descricao: 'Ladinho rssr',
    run: async function (client, message, args) {
        try {
            await client.sendText(message.chatId, `transar de ladinho é mt poggers a piroca entrando e saindo kekw ai tu da uma segurada no peitinho e dale na metelancia mid gap only bota a mão no pescocinho dela e tome micro dif o foda eh so quando o dick escapa da pussy ai é omegalul total`)
        } catch (err) {
            console.error(err)
        }
    }
}